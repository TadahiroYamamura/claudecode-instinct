# Plugin runtime files (stored under .instinct-db/)
.instinct-db/observations.jsonl
.instinct-db/observations.archive/
.instinct-db/.observer.pid
.instinct-db/.observer-signal-counter

# Dolt DB (git-managed separately via dolt push)
.instinct-db/data/
